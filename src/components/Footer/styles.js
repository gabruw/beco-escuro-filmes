//#region Imports

import { COLOR } from '../../assets/styles/color';

//#endregion

export const styles = () => {
    return {
        background: {
            height: '300px',
            backgroundColor: COLOR.PRIMARY,
        },
    };
};

export default styles;
